{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        "$other": {
          ".validate": "$other.matches(/^(claimedRoles|markers|players|roomId|timeline)$/)"
        }
      }
    }
  }
}
